[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "aiq-types"
version = "0.1.0"
description = "Shared domain types for AIQ services"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = [".."]
include = ["aiq_types"]
