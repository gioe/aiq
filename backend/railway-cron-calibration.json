{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "python run_irt_calibration.py",
    "cronSchedule": "0 4 * * 0",
    "restartPolicyType": "NEVER"
  }
}
