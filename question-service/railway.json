{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "/question-service/Dockerfile",
    "watchPatterns": ["/question-service/**", "/libs/**"]
  },
  "deploy": {
    "restartPolicyType": "NEVER"
  }
}
