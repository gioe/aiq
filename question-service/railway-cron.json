{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "question-service/Dockerfile"
  },
  "deploy": {
    "startCommand": "python run_generation.py --count 50 --verbose",
    "cronSchedule": "0 2 * * *",
    "restartPolicyType": "NEVER"
  }
}
